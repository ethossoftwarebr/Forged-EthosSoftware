import { Module } from '@nestjs/common';

import { {{plural className}}Controller } from './{{resourcePath}}.controller';
import { {{plural className}}Repository } from './{{resourcePath}}.repository';
import { {{plural className}}Service } from './{{resourcePath}}.service';

/**
 * {{plural className}}Module — gerado pelo Forge (prompt #9).
 *
 * `PRISMA_CLIENT_TOKEN` é provido globalmente pelo `AuthModule` (@Global) —
 * não precisa declarar aqui.
 */
@Module({
  controllers: [{{plural className}}Controller],
  providers: [{{plural className}}Service, {{plural className}}Repository],
})
export class {{plural className}}Module {}
