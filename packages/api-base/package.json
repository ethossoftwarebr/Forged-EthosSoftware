{
  "name": "@ethos/api-base",
  "version": "0.1.0",
  "private": true,
  "description": "Módulos NestJS reutilizáveis: multi-tenant interceptor, audit log, exception filters, decorators (@CurrentUser, @CurrentTenant, @Roles), pagination/filter helpers. Preenchido nos prompts #7-#8.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'placeholder'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ethos/config": "workspace:*",
    "typescript": "^5.4.0"
  }
}
