{
  "name": "@ethos/types",
  "version": "0.1.0",
  "private": true,
  "description": "Types TypeScript compartilhados entre frontend e backend (Paginated, ApiResponse, helpers de domínio).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'placeholder'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ethos/config": "workspace:*",
    "typescript": "^5.4.0"
  }
}
