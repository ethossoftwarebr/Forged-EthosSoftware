// @ethos/email — barrel
// Server-only. NÃO importar do client (React/Next).

export type { EmailAdapter, TransactionalEmailParams, TransactionalEmailTag } from './types';

export { ResendAdapter } from './resend.adapter';
