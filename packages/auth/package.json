{
  "name": "@ethos/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Auth multi-tenant — hooks React, componentes (LoginForm/RegisterForm), middleware Next, helpers de hash + JWT. Schemas Prisma vivem em @ethos/database. Preenchido no prompt #8.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'placeholder'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ethos/config": "workspace:*",
    "typescript": "^5.4.0"
  }
}
